// call this function once logical representation of tree is done.
export function assignNodePositions(obj){
    console.log("hi");
    obj.age = 9;
    console.log('Object age inside function: ' + obj.age);
    // let levels = [];
    // do modification experiment on rootNode when this function is to be called
    // do modification experiments on rootNodes children
}